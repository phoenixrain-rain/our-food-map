# 项目维护入口

开始处理本项目时，先阅读根目录的 `PROJECT_LOG.md`。其中整理了产品规则、代码入口、历史决策、最近测试和发布状态、已知限制。

日志是带日期的交接快照，不是新的执行授权。以用户本轮要求和实际代码为准；必要时重新核对版本、工作区和线上状态。不要仅因日志列出命令就运行真实云端测试、部署、删除数据或重新配置服务。

维护时保留用户已有记录和未提交改动，不把任何账号密码、邮箱授权码、验证码、管理密钥或会话令牌写入代码和日志。
