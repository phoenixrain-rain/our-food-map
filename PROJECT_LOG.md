# 两个人的美食地图 · 项目日志与维护交接

最后整理：2026-09-18，Asia/Shanghai。当前应用版本：**2.5.0**。

本文用于下一次接续维护，不是待执行的任务清单。线上状态、权限、依赖和测试结果均有时效性；继续工作时以用户最新要求、工作区代码及实际检查为准。不要因为本文记录了部署命令，就自动发布或修改正式数据。

## 1. 先看这里

| 项目 | 当前记录 |
|---|---|
| 产品 | 两个人共同使用的私人美食日记，手机优先 |
| 网站 | https://phoenixrain-rain.github.io/our-food-map/ |
| 本次版本入口 | https://phoenixrain-rain.github.io/our-food-map/?v=2.5.0 |
| GitHub 仓库 | `phoenixrain-rain/our-food-map` |
| 发布分支 | `main`，沿用 GitHub Pages，不是 Netlify |
| 功能提交定位 | `git log --oneline --grep="2.5"`；2.4 提交为 `eb1beb76d1d5c09abebe55eda9b7be60725984f0` |
| Pages 状态 | 2.4 本轮已核查 `built` 且线上验证通过；2.5 发布状态见第 6 节，不据旧版本假定新版本已发布 |
| Supabase 项目 | `ednkwzwxcowxboxmptvs` |
| 最新数据库迁移 | `20260917002000_member_avatars.sql`，上一轮已应用并 dry-run 确认无遗漏 |
| 离线缓存 | `our-food-map-shell-v8` |
| 交付状态 | 2.4 已发布复核；2.5 本机与真实云端测试通过，正在发布，线上结果见第 6 节 |

日志最初建立于 2.3 交付后，本轮新增 2.4 并发布，用户又要求继续迭代直至额度用完，因此接着实现 2.5。此日志和 `AGENTS.md` 已随项目提交；不保存凭据或正式用户数据。没有申请或使用额度重置信用。

## 2. 用户需求与不能改错的产品规则

用户是和女朋友一起使用，女朋友使用安卓手机。需要操作简单、界面精美、双人同步可靠，不要把普通用户操作变成开发者配置流程。

- **一个店铺可以有多次独立打卡。** 一次打卡保存这一顿自己的日期、人均、照片和双方评价；复访不能覆盖上一顿。
- **双方评价同一顿。** 另一半应打开原打卡补评价，不是另发一个副本。两人的评价独立保存，不能互相代改。
- **记录页默认按用餐时间倒序。** 同日按上传时间倒序，缺失用餐日期排最后；可切换其他排序。
- **首页“最近吃过”按首次上传时间倒序。** 修改评分、日期或恢复记录不能把旧记录顶到首页第一条。这与记录页默认排序有意不同。
- **每家店在榜单只出现一次。** 采用用餐日期最近、双方均填完味道/性价比/环境的一次打卡。新打卡没评完时保留以前最近一次完整成绩；不取历史最高分，不跨两次打卡拼接双方评分。
- **评分不能只突出综合分。** 味道、性价比、环境、人均都要清楚展示。主评分为 1–10 分，未评不是 0 分，也不能默认填 8 分。
- 综合分要求三个主评分齐全，权重为味道 50%、性价比 30%、环境 20%；服务、颜值是补充分项。人均 0 元有效，与未填区分；想吃的预算不计实付统计。
- 删除是**可恢复的软删除**，只影响这一顿。两位成员都可操作；回收站保留评价和照片，恢复后仍属于原店铺。
- 分类有常用建议，也允许最长 40 字的自定义内容；不重写历史分类。
- 店铺以稳定 `place_id` 识别，不能按同名自动合并不同分店。手工调整归属也只改这一顿，不合并评价或照片。
- 头像可使用个人照片并同步给另一半，图片不能因此变成公开资源。
- 保留用户主动双指缩放；修复输入聚焦放大时不要设置 `user-scalable=no`。

### 到访徽章

| 实际到访次数 | 称号 | 配色 |
|---|---|---|
| 0 | 待初访 | 中性灰 |
| 1 | 初见 | 绿 |
| 2–3 | 回头客 | 蓝 |
| 4–7 | 熟客 | 紫 |
| 8–14 | 常客 | 金 |
| 15 及以上 | 私藏宝店 | 玫瑰 |

只统计该店未删除的“吃过”打卡，想吃计划、两人分别评价和重复 ID 不额外计次。详情显示下一级所需次数。它是你们自己的到访纪念，不是商家会员权益。

## 3. 已完成的阶段

| 阶段 | 提交 | 主要结果 |
|---|---|---|
| 登录调整 | `25b8bc0` | 邮件链接登录改为在原页面输入邮箱验证码；用户后来确认成功登录并创建空间 |
| 2.0 | `508e7f4` | 重做照片保存与失败重试、主评分/人均、账号隔离、双人权限、成员显示、本机备份与离线支持 |
| 2.1 | `18cb5d0` | 回收站删除/恢复、双方完整评分才入榜、上传时间排序、自定义分类、本机多标签事务保护 |
| 2.2 | `d9c4b77` | 独立店铺身份、同店复访历史、记录按用餐日期排序、单店单榜位、实时通知漏收的前台轮询兜底 |
| 2.3 | `6a2abe2` | 私有自定义头像、裁剪预览、彩色到访徽章、奶油白深绿界面、搜索字号修复、照片稳定更新 |
| 2.4 | 查看 Git 的 2.4 功能提交 | 城市/分类/价格/日期筛选、按店去重且一轮不重复的选店抽签、月度日历食记、餐厅信息安全复制 |
| 2.5 | 查看 Git 的 2.5 功能提交 | 待办中心、含 JPEG 照片的本机草稿、恢复时版本保护、退出清理隐藏私有内容、应用内使用指南、发布资源自检 |

详细记录：[2.0](docs/update-2.0.md)、[2.1](docs/update-2.1.md)、[2.2](docs/update-2.2.md)、[2.3](docs/update-2.3.md)、[2.4](docs/update-2.4.md)。旧日志中的规则以新版本覆盖，例如 2.1 的记录默认上传排序、按帖子排榜，已在 2.2 改为当前规则。

设计研究参考及原始链接留在各版本日志中，包括 Beli、Day One、Apple 手记、Swarm、Polarsteps、Letterboxd 等。借鉴信息组织和私人记录体验，不复制产品素材，也不把本项目算法声称为其他产品的算法。

2.5 细节：[待办中心与本机草稿](docs/update-2.5.md)。本轮主动暂存不是自动草稿或离线云端写入队列；草稿不会自动上传给另一半。

## 4. 代码地图

这是静态 HTML/CSS/JavaScript ESM 项目，不是 React 工程，不需要打包应用才能部署。Supabase 浏览器 SDK 随仓库提供，避免运行时依赖外部 CDN。

| 文件 | 用途 / 排查入口 |
|---|---|
| `index.html` | 四个主页面、导航、编辑/详情/登录/空间/头像等弹层；稳定 DOM ID 被测试引用 |
| `styles.css` | 移动端布局、应用固定外壳、内部滚动区、2.3 配色与组件样式；有历代覆盖规则，改动需核对最终样式 |
| `app.js` | 状态、登录、渲染、表单、图片草稿、上传、备份、成员与同步协调 |
| `lib/model.js` | 纯数据规则：评分、日期排序、筛选、店铺分组、入榜选择、到访等级、备份验证 |
| `lib/cloud.js` | Supabase 查询、RPC 保存、私有图片签名缓存、餐厅照片/头像清理队列 |
| `lib/local-store.js` | IndexedDB 快照、旧数据迁移、事务内读改写 |
| `lib/photos.js` | 图片解码兼容、方向处理、压缩、data URL 转换 |
| `lib/avatar.js` | 头像加载、圆框所见区域的正方形裁剪、512×512 JPEG 输出 |
| `lib/dom.js` | HTML 无变化时跳过更新；有变化时原位更新节点，保留已解码图片 |
| `lib/discovery.js` | 精细筛选、按店抽签、近七日判断、月历与月报、可复制信息的数据规则 |
| `lib/discovery-ui.js` | 筛选/抽签/月报/复制的界面协调，账号切换时清空派生私有内容 |
| `lib/tasks.js` | 待办分类、双方归属、缺日期/人均/照片地址的数据检查 |
| `lib/drafts.js` / `lib/draft-ui.js` | 设备本机草稿、事务 revision、7 天过期、账号定向清理、暂存/恢复/删除交互 |
| `service-worker.js` | 应用外壳离线缓存、版本更新、只清理本应用的旧缓存 |
| `manifest.webmanifest` | PWA 桌面安装、主题色、图标和启动地址 |
| `vendor/` | 本地 Supabase SDK 与许可证说明 |
| `supabase/migrations/` | 数据库结构、事务函数和行级权限的增量变更 |
| `tests/model.test.js` | Node 数据规则测试 |
| `tests/app.ui.spec.js` | 记录、照片、排序、复访、离线、备份等浏览器回归 |
| `tests/avatar.ui.spec.js` | 头像、搜索缩放、图片节点稳定、并发档案修改和导出测试 |
| `tests/discovery.test.js` / `tests/discovery.ui.spec.js` | 2.4 数据边界、交互、离线与小屏布局回归 |
| `tests/tasks-drafts.test.js` / `tests/drafts.ui.spec.js` | 待办口径、草稿 Blob、并发、过期、冲突、失败重试和离线恢复 |
| `tools/check-release.mjs` | 版本、模块引用、离线清单、HTML ID/标签和缩放的只读发布自检 |
| `tools/cloud-check.mjs` | 使用隔离临时账号/空间的真实云端集成检查 |
| `tools/serve.mjs` | 仅监听本机的静态预览，默认端口 4173 |
| `.private-audit/` | 本地检查脚本、截图和必要的测试资源编号；已被 Git 忽略，不保证在新设备存在 |

### 关键状态与交互

- `mode` 区分 `loading`、`local`、`cloud`。云端保存失败不得悄悄降级保存到本机，让用户误以为另一半已收到。
- `authEpoch` 阻止旧账号的迟到异步结果混进新账号界面；退出登录清理云端显示、图片链接与相关草稿。
- 本机数据库为 `our-food-map`，对象仓库 `snapshots`，快照键 `local`。旧 localStorage 键 `couple_food_mobile_v2` 只用于首次迁移，原数据保留。
- 本机修改用 `mutateLocal` 在同一 readwrite 事务内读取最新快照再更新；不要退回整份旧内存快照覆盖写入。
- 云端 Realtime 监听 `restaurants`、`reviews`、`food_photos`、`space_members`。监听就绪/重连触发重新读取；页面可见且在线时每 20 秒兜底核对。
- `refreshCloud` 比较新旧快照，无变化不整页重绘。`renderHTML` 只更新必要节点，不要重新引入每轮同步 `innerHTML` 重建所有照片。
- 记录页按店铺收纳/逐次打卡均保留；详情和列表展开的历史不应被后台同步折叠。
- 精细筛选先筛各次打卡再分店；月报按用餐日期，只统计未删的吃过记录。平均人均不是两人总花费；未知金额不当 0。
- 抽签每店一张签，吃过按最近一次快照，想吃按最新上传计划；混合来源同店有计划时优先计划。近七日排除检查该店所有吃过记录。
- 新界面的筛选与抽签偏好只属于当前会话；账号切换时连隐藏弹层内容和城市/分类选项都清除，不向下一账号泄露上一个账号的摘要。
- 草稿独立数据库 `our-food-map-drafts` / 仓库 `drafts`，按本机或云端用户+空间分开。只在用户主动暂存时写入，每档案一份；7 天后读取时清理。正式导出备份不包含草稿。
- 草稿保存实际 JPEG Blob 和表单字段，不保存签名链接、object URL 或令牌。恢复保留原记录 `updated_at`，过期草稿不能覆盖另一页面或另一半的新内容；正式保存只清理同 revision 的草稿。
- 退出时清理原账号草稿，并清空隐藏详情、成员、邀请码、表单、图片和裁剪画布。清理失败要明确提示，不假称完成；不要删除独立本机档案或另一账号草稿。

## 5. 云端数据与图片约束

主要数据表：

- `spaces`：私有双人空间、邀请码。
- `space_members`：成员、昵称、角色、`avatar_path`；最多两人，当前模型每个账号只加入一个空间。
- `food_places`：稳定店铺身份，以空间隔离。
- `restaurants`：**实际保存每次打卡快照**，不要被表名误导成唯一店铺资料；含 `place_id`、用餐日期、上传/更新时间与软删除字段。
- `reviews`：每人每次打卡一条评价。
- `food_photos`：某次打卡的照片元数据和所属用户。

主要写入入口：`create_space`、`join_space_by_code`、`save_food_record`、`set_food_record_deleted`、`save_member_profile`。

- 打卡保存与删除使用一致的事务锁和版本检查；照片先暂存上传，再原子保存打卡/自己的评价/照片关联。
- 头像 RPC 校验账号、成员身份、旧昵称/旧头像路径、文件归属和存在性；不授予客户端直接修改头像列的权限。
- `food-photos` 是私有桶，上传路径为 `space/visit/user/uuid.jpg`。每人每次打卡最多 8 张；输入单张最大 30 MB，压缩最长边 1800，输出 JPEG 最大 5 MB。
- `food-avatars` 是独立私有桶，路径为 `space/user/uuid.jpg`，只接受 JPEG，最大 1 MiB；前端输出 512×512，不上传原始照片或原图 EXIF。
- 签名 URL 有效期 1 小时，前端复用约 50 分钟；失败/缺失文件负缓存 5 分钟，手动刷新可立即重试。签名 URL 不是可长期保存的图片内容。
- 替换照片/头像使用新路径，旧文件通过清理队列处理。删除前重新检查关联，尤其要防止“保存已提交但响应丢失”后误删成功保存的新图。
- 回收站删除不会物理清理该次打卡的评价和照片；不要把正常替换图片的清理与删除记录混为一谈。
- 备份导出嵌入可读取图片；导入仅在本机模式合并，不覆盖已存在的同编号记录，也不复活已删记录或携带原空间权限。

增量迁移顺序：

1. `20260916000000_initial_schema.sql`
2. `20260916001000_fix_invite_code_generator.sql`
3. `20260916002000_reliable_records_and_photos.sql`
4. `20260917000000_record_trash.sql`
5. `20260917001000_place_visits.sql`
6. `20260917002000_member_avatars.sql`

维护时增加新迁移，不修改已经应用的历史迁移来假装线上已更新。

## 6. 最近一次验证结果

### 2.5 本轮（2026-09-17 至 2026-09-18）

- 2026-09-18 重新完整执行：30 项 Node 测试、36 项浏览器测试通过，共 66 项。含 2.4/2.3 所有既有回归；新增草稿文字/真实 Blob 重开、暂存失败不丢输入、替换确认、旧版本冲突、事务竞争、按账号清理、离线恢复、待办归属与完成更新，以及小屏使用指南和键盘打开/关闭。
- `npm run check:release` 通过：12 个前端模块、20 项离线资源，检查版本/路径/缓存、DOM ID、标签关联和手动缩放。
- 已人工查看草稿卡、待办中心截图；320/393/1280px 布局检查通过。
- 2026-09-18 `npm run test:cloud` 完整通过，退出码 0。两位隔离账号实际验证了草稿 JPEG 不提前上传、重开恢复、正式发表后对方实时可见、待办不代改对方评价、退出清除本账号草稿及隐藏私有 DOM。所有原有权限、头像、照片、复访、回收站、双人榜单和 Realtime 失效后的轮询兜底也通过。
- 测试结束已清理临时账号、空间和图片，原有六张数据表行数前后一致。此次运行前只读检查未发现旧测试账号遗留。
- 前一日初轮在旧有照片 `load` 次数断言处失败（同一个图片节点仍保留）；检查发现断言计数开始可能早于初次加载事件，补齐首次 decode/绘制等待及不含私有 URL 的诊断，未放宽“无额外 load”断言，本轮已通过。
- 前一日另遇到临时网络 fetch 失败。只对列举存储对象的只读请求增加有限重试；测试账号创建前记录待确认邮箱，在响应丢失时仅按本次标记定向核对清理，避免遗留临时账号。没有对正式写入盲目重试。
- 2026-09-17 `npm audit --registry=https://registry.npmjs.org --json` 报告 0 个已知漏洞，是当时依赖数据库快照，不代表不存在所有安全风险。
- 不需要新增数据库迁移。
- 发布与线上检查：待本轮 Pages 构建完成后补记，不能把上述本机/云端通过当作已经上线。

### 2.4 本轮（2026-09-17）

- `npm test`：27 项通过；`npm run test:ui`：29 项通过，总计 56 项。
- 已验证组合筛选、无效区间、0 与空金额区别、每店一次抽签、一轮不重复、近七日排除、闰年月历、当天照片与打卡筛选、复制失败手动退路、离线删除后统计更新。
- 已检查 320/393/1280px 新面板无横向溢出，移动尺寸截图已人工查看；新输入框保持至少 16px。
- `npm run test:cloud` 完整通过：原有权限、照片、头像、复访、回收站和离线通知兜底全部复测；新增月报打开时接收另一人的日期修改、按店抽签、城市/照片筛选、退出后隐藏摘要与选项清空验证。
- 隔离临时账号、空间、图片已清理；原有六张数据表行数前后一致，无遗留测试资源文件。
- 本轮不需要数据库迁移，没有重新配置登录或 SMTP。
- `eb1beb7` 已推送，Pages 状态 `built`、错误为空；线上 2.4 验证脚本通过，包含筛选、月报、抽签、安全复制、头像、离线、删除恢复、复访徽章和时间线。

### 2.3 历史验证

以下为 **2.3 发布轮次**已完成的结果，不是每次打开日志就自动保持有效：

- `npm test`：19 项通过。
- `npm run test:ui`：22 项通过，合计 41 项自动测试。
- `npm run test:cloud`：真实云端隔离空间回归通过。覆盖成员与越权、私有图片、头像大小限制、独立评分、并发保护、上传失败重试、提交后响应丢失、旧图片清理、复访与榜单、删除恢复和退出隔离。
- 双账号浏览器验证了头像同步，以及后台更新不替换已加载照片、不重复触发原图片的 load 事件。
- 阻断所有 Realtime WebSocket 消息后，前台兜底核对仍能同步另一人的修改。
- 临时账号、空间、打卡和图片已清理；正式数据表前后行数一致。测试资源日志已清理，没有遗留待处理测试资源。
- 手机尺寸和桌面截图已检查：首页、记录、详情、档案、头像设置、回收站等。
- GitHub Pages 构建成功后，线上脚本验证了 2.3 资源、搜索字号、头像裁剪与本机持久化、导航、离线打开/保存、删除恢复、复访徽章与时间线。

**测试边界：** 使用 Windows Edge/Chromium 的触屏与手机尺寸模拟，不是用户实体安卓手机或 iPhone 的真机验证；没有证明所有设备均无问题。以前版本日志中的依赖安全审计结果属于当时快照，2.3 本轮没有据此声称重新做过审计。

## 7. 开发与复测

工作目录为项目根目录。当前环境是 Windows PowerShell；已用过 Node 24.15.0、Edge、Git、GitHub CLI、Supabase CLI，换设备先核实路径与登录状态。

```powershell
git status --short
git log -5 --oneline
npm ci
npm test
npm run test:ui
npm run check:release
npm run serve
```

预览默认 `http://127.0.0.1:4173`；浏览器测试会启动预览。需要与云端集成测试并行时，浏览器 UI 测试可以在独立 shell 中设置 `$env:PORT='4174'`。云端测试使用 4173，不要让两个真实云端测试同时运行，以免前后数据计数互相干扰。

当前设备可用的网络代理曾为 `http://127.0.0.1:7897`，属于环境快照，不是必须存在的项目服务。网络失败时先检查代理是否运行，不要无限重复请求或擅自修改全局代理。

### 真实云端测试的注意事项

这不是只读检查：会在真实 Supabase 项目创建和清理临时测试资源。仅在本轮任务确实需要并有相应授权时运行。

```powershell
$env:HTTPS_PROXY='http://127.0.0.1:7897'
$env:HTTP_PROXY='http://127.0.0.1:7897'
$env:NO_PROXY='127.0.0.1,localhost,ednkwzwxcowxboxmptvs.supabase.co'
$env:NODE_USE_ENV_PROXY='1'
npm run test:cloud
```

脚本要求当前机器的 Supabase CLI 已登录并具有对应项目权限。使用确认过的临时测试账号，不发送真实测试邮件。管理凭据仅存在进程内存，不打印、不写入日志。

异常退出时先查看 `.private-audit/cloud-resources-*.json` 的测试资源编号，确认空间名称与该次测试标记一致；按脚本中的定向清理方式收尾。不要为了让计数通过而清空整张表或整个存储桶；等清理结束后再重跑。

## 8. 发布维护清单

下面是有发布授权时的操作参考。用户本轮要求实现并继续完善现有网站，沿用现有 Pages 发布流程；以后仅查阅日志不构成发布授权。

1. 检查工作区，保护用户未提交改动；阅读相关模块和测试，不凭旧日志假定当前代码。
2. 完成适当的模型、浏览器和云端验证；新迁移先 dry-run，确认目标项目与变更范围再应用。
3. 修改应用资源时统一升级版本：`package.json`、lockfile、HTML 的脚本/CSS 引用、各模块 import、Service Worker 资源清单；缓存名称继续递增。新增前端模块必须加入离线清单。
4. 更新 SDK 依赖才需要 `npm run vendor`，连同许可证说明检查。普通静态页面改动不需要重新构建 SDK。
5. 检查差异、敏感信息和文档。`.private-audit`、测试输出、令牌、邮箱授权码、服务角色密钥都不能提交。
6. 沿用现有仓库和 `main` 发布。不要重新建仓库、迁移平台或让用户重新配置 SMTP。
7. 等待 Pages 对应提交达到 `built` 后，再核查真实网址；只成功 push 不等于网站已完成部署。
8. 验证离线缓存、导航和新功能；给用户版本链接。仍见旧页面时先关闭旧标签/PWA 窗口再重开，不让用户随意清空含未备份本机数据的站点存储。

参考命令（需先确认本轮范围）：

```powershell
npx --yes supabase@latest db push --dry-run
# 仅在确认迁移与权限后：
npx --yes supabase@latest db push --yes

git diff --check
# 审核并提交本轮相关文件后：
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main
gh api repos/phoenixrain-rain/our-food-map/pages/builds/latest --jq '{status: .status, commit: .commit, error: .error.message}'
```

本机曾使用 `.private-audit/check-live.mjs` 做线上验证；它被忽略，不应假设新克隆一定包含该文件。该脚本最后一次运行成功，使用全新隔离浏览器的本机档案，不写正式用户的云端空间。

## 9. 已知边界与常见排查

| 现象 / 边界 | 已知情况与下一步 |
|---|---|
| 个别历史照片始终不可用 | 2.0/2.1 曾发现 3 条元数据没有对应存储文件；本次没有重新逐张审计。没有原文件无法还原，需用户重新上传，不要虚构恢复成功 |
| 历史同店同日副本 | 2.1 曾发现疑似旧重复发帖，未自动合并或删除；需要用户核实后手工整理，不能仅按店名认定重复 |
| 历史默认 8 分 | 无法可靠区分旧自动填分与真实主动评分，因此保留，不做批量改写 |
| 搜索时页面放大 | 先查实际输入字号、加载的 CSS 版本、系统字号与浏览器设置；2.3 输入至少 16px，保留手动缩放 |
| 照片持续闪动 | 检查是否退回同步时重建 DOM、签名链接是否每次重新生成、图片是否缺失；正常签名到期更新与持续重载不同 |
| 另一半看不到新内容 | 确认同一空间和成员状态、云端保存确实成功、Realtime 及可见页面轮询、登录权限和网络；不要用本机模式假装同步成功 |
| 云端离线保存 | 当前不支持离线云端写入队列；可查看已加载内容，保存需联网。本机档案可离线保存 |
| 手机安装 | 当前是可添加桌面的 PWA，不是原生 APK，也未上架应用商店 |
| HEIC 等格式 | 取决于浏览器原生解码能力；失败时提示转换 JPG/PNG/WebP，没有新增专用 HEIC 转换服务 |
| 云端头像设置 | 当前绑定已加入空间的成员；先加入空间才能保存云端头像，本机头像与云端头像分开 |
| SMTP / 邮箱 | 用户曾配置 QQ SMTP 并确认成功登录。未来送信失败需重新核对服务配置与限额；不要从旧聊天抄取或复用授权码 |

本轮用户要求在完成一批后继续完善并写入日志。2.4 后已经实现待办整理与编辑草稿保护，最终验证发布状态见第 6 节。没有创建无人值守自动化或申请额度重置。真机反馈、历史照片补传或正式记录整理都需要具体证据/素材，不能当作已获授权的自动修复任务。

## 10. 下次接手顺序

1. 阅读本日志头部状态与用户最新要求。
2. 检查 `git status`、HEAD、`package.json`、迁移目录；涉及线上状态时重新确认。
3. 在上方代码地图定位相关模块，再看对应测试和版本日志。
4. 尽可能用合成数据重现，不动正式用户记录；明确区分“检查”“实现”“发布”的授权范围。
5. 修改后按风险复测，并记录本轮确实验证过的结果、未验证范围及已知遗留。

日志维护应写日期、版本、提交、改动原因、验证证据和限制，不写密码、验证码、授权码、私有图片签名地址或可用会话令牌。
